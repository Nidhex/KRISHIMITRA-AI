{
  "name": "krishimitra-ai-monorepo",
  "version": "1.0.0",
  "description": "KrishiMitra AI Monorepo Deployment",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && (pip install -r ../requirements.txt || pip3 install -r ../requirements.txt || true)",
    "start": "cd backend && npm start"
  }
}
